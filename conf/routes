# Encore
# ~~~~~

# Artists
GET     /events/past.json              controllers.Application.pastEvents(artist_id: String)

# Events
GET     /events/:event_id.json         controllers.Application.singleEvent(event_id: Long)
